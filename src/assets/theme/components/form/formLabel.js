// Material Kit 2 React base styles
import colors from "assets/theme/base/colors";

const { text } = colors;
// eslint-disable-next-line 
export default {
  styleOverrides: {
    root: {
      color: text.main,
    },
  },
};
